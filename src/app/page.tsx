import { GameClient } from '@/components/game-client';

export default function RiskyRevelationsPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-4 md:p-8">
      <GameClient />
    </main>
  );
}
